import { MOSTRAR_ALERTA, OCULTAR_ALERTA } from "../types/index";

const inicialState = { alerta: null };

export default function (state = inicialState, action) {
	switch (action.type) {
		default:
			return state;
	}
}
